# ── Contact Form (Nodemailer / cPanel SMTP) ──────────────────
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_USER=contact@yourdomain.com
SMTP_PASS=your_email_password_here
CONTACT_EMAIL=your@gmail.com

# ── Admin Panel ──────────────────────────────────────────────
# Change this from the default! Used to log in at /admin
ADMIN_PASSWORD=admin123

# ── GitHub API (optional — raises rate limit to 5000/hr) ─────
GITHUB_TOKEN=
GITHUB_USERNAME=devkarasel

# ── Social Links (update in components/Contact.tsx & Footer.tsx)
# Replace devkarasel with your actual handles
